name: example
description: Demonstrate how Feels Golden can be used for widget testing
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  feels_golden:
    path: ../

flutter:
  uses-material-design: true
  assets:
    - images/
